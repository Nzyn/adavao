services:
  - type: web
    name: alertdavao-admin
    env: php
    plan: free
    region: singapore
    buildCommand: cd AdminSide/admin && composer install --no-dev --optimize-autoloader
    startCommand: cd AdminSide/admin && php artisan migrate --force && heroku-php-apache2 public/
    envVars:
      - key: PHP_VERSION
        value: 8.1
